# Design Guardrails

このファイルは「スクロールでわかる 日本のれきし」のデザインを守るための必須ルールです。作業者は、デザインや表示に関わる変更を行う前に必ず読むこと。

## 1. 変更前に許可が必要なもの

次の変更は、重要箇所として必ずユーザーの許可を取る。

- `styles.css` の大きな変更
- トップタイトル、ヒーロー画像、背景、フォントサイズ、余白、カード幅、カード高さの変更
- 7つの大区切りセクションのレイアウト変更
- 西暦表示、縦ライン、時代カードの位置に関わる変更
- アコーディオンの開閉位置や動きの変更
- 人物カード、アクションカード、詳細本文のデータ構造変更
- モーダルのデザイン変更
- `design-baseline/styles.baseline.css` の更新
- 復元スクリプト、バックアップ仕様の変更

小さな誤字修正や明確な表示バグ修正でも、上記に触れる場合は先に確認する。

## 2. 文字色と背景色の決定ルール

文字色は、背景色を把握してから決める。背景がグラデーション、画像、半透明、時代ごとの色変化を持つ場合は、単一の固定色だけで判断しない。

必須ルール:

- 赤文字を濃い緑、濃い青、濃い茶、濃い紫の背景に直接置かない。
- `.eyebrow` のような強調文字は、暗い背景では白または白に近い色を使う。
- 明るい背景では `var(--ink)` または十分に濃い色を使う。
- 背景が `linear-gradient()` の場合は、開始色と終了色の両方で読みやすさを確認する。
- 背景画像の上に文字を置く場合は、画像の明暗差を考え、必要ならオーバーレイを調整する。
- 半透明背景の上に文字を置く場合は、背後の色も含めて見え方を確認する。

推奨仕様:

- 通常本文は、背景とのコントラスト比 4.5:1 以上を目安にする。
- 大きな見出しでも、コントラスト比 3:1 未満にはしない。
- 時代カードの濃色ヘッダー内では、見出し・本文・年代表記を原則として白系にする。
- 赤系の文字は、白または薄いクリーム色の背景でのみ使用する。
- `color-mix()` や半透明色を使う場合は、実際の背景に乗った状態で確認する。

禁止例:

```css
.era-head .eyebrow {
  color: #7b2d26;
}
```

濃い背景の時代カード内では赤文字が沈むため禁止。必要な場合は次のように場所を限定する。

```css
.era-head .eyebrow {
  color: rgba(255, 255, 255, .88);
}
```

## 3. デザインルール

このサイトのデザインは、小学生が歴史の流れを迷わず読めることを最優先にする。装飾は学習の補助に限定し、本文、カード、年表、開閉UIの読みやすさを壊してはいけない。

### 基準フォント

基準フォントは次の3つとする。

- `Noto Sans JP`
- `Noto Serif JP`
- `Shippori Mincho`

CSSでは次の役割で使い分ける。

```css
--font-sans: "Noto Sans JP", "Yu Gothic", "Hiragino Kaku Gothic ProN", Meiryo, system-ui, sans-serif;
--font-serif: "Noto Serif JP", "Shippori Mincho", "Yu Mincho", "Hiragino Mincho ProN", serif;
--font-mincho: "Shippori Mincho", "Noto Serif JP", "Yu Mincho", "Hiragino Mincho ProN", serif;
```

使用ルール:

- 本文、ボタン、カード内テキスト、検索、タグは `Noto Sans JP` を基準にする。
- トップタイトル `h1` は `Shippori Mincho` を基準にする。
- セクション見出し `h2` は `Noto Serif JP` を基準にする。
- 長文本文に明朝体を使わない。小学生向けの読みやすさを優先する。
- フォントサイズを viewport 幅だけで極端に拡大縮小しない。
- 文字間隔は原則 `letter-spacing: 0` とし、詰めすぎない。
- フォント変更時は、漢字、ひらがな、ルビ、ボタン文字が崩れていないか確認する。

### 色

- 1画面内で主役になる色を増やしすぎない。
- 時代カードは時代ごとの色を使ってよいが、文字色は背景とのコントラストを優先する。
- 赤は注意・強調の色として扱い、濃色背景上の本文色には使わない。
- 白背景や薄いクリーム背景では `var(--ink)` を基本文字色にする。
- 濃いグラデーション背景では白系文字を基本にする。
- 半透明の白背景を使う場合は、背後の色により文字が薄く見えないか確認する。

### 余白とサイズ

- 7つの大区切りカードは、縦に長すぎないようにする。
- 大区切りカードの高さは、見出し、時代名、キーワード、開閉ボタンが読み取れる範囲で最小限にする。
- 余白は情報のまとまりを分けるために使い、装飾目的で大きくしすぎない。
- 枠線や背景を持つコンテンツ領域では、枠と本文テキストの内側余白を必ず15px以上確保する。
- 15px未満の余白を使える例外は、アイコンボタン、タグ、チップ、タイムラインバーなどの小さな操作・ラベル部品に限る。
- カード内にカードを入れ子にしない。
- 固定形式のUIは、`min-height`、`grid-template-columns`、`gap` などで安定させる。
- クリックや開閉で、`くらし`、`できごと`、`大きな力` のカード位置を不自然に動かさない。

### カード

- カードの角丸は原則 `8px` 以内にする。
- 影は薄く使い、カードが浮きすぎないようにする。
- 人物カード、アクションカード、時代カード、詳細カードの役割を混ぜない。
- 詳細アコーディオンは、押したカード内ではなくカードの下に表示する。
- 時代カード下の詳細本文は、時代カードの `くらし`、`できごと`、`大きな力` と同じフォントサイズ基準で表示する。
- 子カテゴリー画像は高さ200pxで表示し、`object-fit: cover` で中央トリミングする。画像の原寸高さでカードを縦長にしない。
- カード内の文字がはみ出す場合は、文字サイズだけでなく幅、折り返し、余白を調整する。

### 年表と現在地

- 左側の西暦表示を消さない。
- 西暦表示は縦ラインやカードに隠れない位置に置く。
- 縦ライン、時代アイコン、時代カードの位置関係を崩さない。
- 7つの大区切りにも西暦範囲を表示する。
- モバイル幅では、西暦が見切れないように横幅と左余白を調整する。

### 開閉UIとボタン

- アコーディオンや詳細開閉は、一般的なWeb UIとしてシェブロンアイコンを使う。
- 開閉UIに「ひらく」「とじる」の文字ラベルを常時表示しない。
- 閉じている状態は下向きシェブロン、開いている状態は上向きシェブロンで示す。
- シェブロンは `.disclosure-icon` を使い、CSSで回転させて状態を示す。
- 見た目がアイコンだけの場合は、必ず `aria-label` または親要素の文脈で意味が伝わるようにする。
- 開閉ボタンのアイコン色は背景と同化させない。
- 主要操作のボタンは最低44px程度の押しやすさを確保する。
- アイコンだけのボタンには `aria-label` を付ける。
- ホバーやアニメーションは控えめにし、カード位置をずらさない。

### ルビ

- 一般的ではない名前や単語にはルビを付ける。
- 小学5年生までに習わない漢字を含む語にはルビを付ける。
- つながっている熟語は、必要に応じて単語全体にルビを付ける。
- ルビで行高が崩れないか、カードとボタンで確認する。

### モーダル

- 人物カードとアクションカードは同じモーダルUIを使う。
- 現在のモーダルデザインは承認済み扱いとし、変更する場合は必ず許可を取る。
- モーダル内の本文は読みやすい行幅にする。
- 閉じるボタンは常に見える位置に置く。

### 禁止するデザイン変更

- 読みやすさより装飾を優先する変更
- 背景色を確認しない文字色変更
- カードやセクションを不必要に縦長にする変更
- 既存の学習導線を説明なしに移動・削除する変更
- 復元基準を確認なしに更新する変更
- モーダル以外の大きな見た目変更を許可なしに行う変更

## 4. デザイン変更時に確認する画面

デザイン変更後は、最低限次の表示を確認する。

- トップタイトルが大きすぎない、細すぎない、画像に埋もれていない
- 7つの大区切りカードが縦に長すぎない
- 大区切りカード、人物図鑑、詳細カードのシェブロンが見やすい
- 左側の西暦表示が消えていない、見切れていない
- 縦ラインと時代カードの位置がずれていない
- 各時代カードの赤文字・白文字が背景に同化していない
- `くらし`、`できごと`、`大きな力` のカード位置が開閉で不自然に動かない
- 詳細アコーディオンがカードの下に表示される
- 人物カードとアクションカードのモーダルが開く
- モバイル幅でも文字がはみ出さない

## 5. バックアップ仕様

`styles.css` を変更する前に、必ずバックアップを残す。

復元用の基準:

- `design-baseline/styles.baseline.css`

復元コマンド:

```powershell
.\scripts\restore-design-baseline.ps1
```

このコマンドは、現在の `styles.css` を `design-baseline/restore-backups/` に退避してから基準版へ戻す。

基準更新コマンド:

```powershell
.\scripts\save-design-baseline.ps1
```

このコマンドは、以前の基準版を `design-baseline/baseline-backups/` に退避してから、現在の `styles.css` を新しい基準版にする。

重要:

- 基準更新は、ユーザーが見た目を確認して承認した後だけ行う。
- 作業途中の状態を基準版にしてはいけない。
- `script.js` の人物データや本文データは、デザイン復元の対象に含めない。
- データ変更が必要な場合は、デザイン復元とは別作業として扱う。

## 6. 修復作業の手順

デザイン崩れを修復するときは、次の順で進める。

1. どこが崩れているかをスクリーンショットまたはユーザー説明から特定する。
2. `DESIGN_RESTORE.md` とこのファイルを読む。
3. 重要箇所に触れる場合は、修正前にユーザーの許可を取る。
4. `styles.css` を変更する前にバックアップを残す。
5. 変更範囲を最小にする。
6. トップ、7つの大区切り、西暦表示、時代カード、詳細、モーダルを確認する。
7. ユーザー確認前に復元基準を更新しない。

## 7. 守るべき考え方

このサイトは小学生が歴史の流れを読むための教材である。装飾よりも、読みやすさ、現在地の分かりやすさ、開閉時の安定、文字の見やすさを優先する。

デザイン変更は「きれいにする」ためではなく、「読みやすく、迷わず学べる状態を守る」ために行う。

## 8. 人物カードの仕様

人物カードは、歴史を動かした具体的な人物を学ぶためのカードである。単に有名な人を並べるための一覧ではない。

データ構造:

```js
[
  "人物名",
  "ふりがな",
  "主に活躍した時代",
  "関連する場所・組織・分野",
  "短い肩書き",
  "説明文",
  "アイコン"
]
```

必須仕様:

- 人物名は実在する個人に限定する。
- 「藤原氏」「平氏」「源氏」「元」「幕府」「朝廷」「明治政府」など、個人ではない集団・制度・国・事件は人物カードにしない。アクションカードとして扱う。
- 人物は、主に活躍した時代だけに表示する。時代をまたいで重複表示しない。
- 複数時代に関わる人物でも、教材上もっとも重要な活躍時代を1つ選ぶ。
- 人物名、一般的ではない名前、5年生までに習わない漢字を含む語にはルビを付ける。
- 説明文は50字以上150字以内を目安にする。
- 説明文は史実に基づき、具体的に何をした人物かが分かる内容にする。
- 「当時の政治・文化・社会の変化を考える手がかりになります。」のような汎用的な説明文は使わない。
- 評価が分かれるだけで社会的影響が教材上説明しにくい人物、単なる有名人、流行・知名度だけの人物は追加しない。
- 追加する場合は、その人物がその時代を理解するために必要かを確認する。

表示仕様:

- 人物カードは「時代を動かした人たち」の中に表示する。
- 時代ごとの人物一覧はアコーディオンで閉じる。
- 全体の人物図鑑では、ジャンルごとに分類して表示する。
- 検索、ジャンルフィルター、お気に入りに対応する。
- カードにはアイコン、人物名、ふりがな、時代、ジャンル、短い肩書きを表示する。
- クリックするとモーダルで詳細を表示する。
- モーダルには、アイコン、時代、人物名、短い肩書き、説明文、タグを表示する。
- モーダルデザインは現在の仕様を維持し、変更する場合は必ずユーザーの許可を取る。

ジャンル分類:

- 政治・国づくり
- 武士・軍事
- 文化・学問
- 宗教・思想
- 産業・社会
- 科学・技術
- スポーツ・記録
- そのほか

ジャンルは説明文、肩書き、関連分野のキーワードから判断する。分類があいまいな場合は、教材上どの文脈で学ばせたいかを優先する。

追加・削除時の確認:

- その人物が本当に個人か確認する。
- 主な活躍時代が正しいか確認する。
- 説明文が50字以上150字以内か確認する。
- 史実に反する表現や過度な評価表現がないか確認する。
- 人物名と難しい語にルビがあるか確認する。
- 既存人物と重複していないか確認する。
- 時代ごとの表示数が極端に少なくならないか確認する。

## 9. アクションカードの仕様

アクションカードは、人物ではない重要語、出来事、制度、集団、文化、戦い、国、社会変化を説明するためのカードである。詳細本文中の太字語をクリックしたときに、人物カードと同じモーダルで表示する。

データ構造:

```js
"用語名": [
  "短い分類・要約",
  "説明文",
  ["タグ1", "タグ2"]
]
```

アクションカードにするもの:

- 出来事: `応仁の乱`、`大化の改新`、`元寇` など
- 制度・政治のしくみ: `律令国家`、`摂関政治`、`院政`、`幕府`、`廃藩置県` など
- 集団・家・勢力: `藤原氏`、`平氏`、`源氏`、`北条氏`、`明治政府` など
- 国・外部勢力: `元`、`魏`、`明`、`アメリカ` など
- 文化・技術・道具: `土器`、`かな文字`、`浮世絵`、`鉄道`、`テレビ`、`インターネット` など
- 社会変化: `米づくり`、`高度経済成長`、`戦後復興` など

人物カードにしてはいけないもの:

- 氏族、家、集団、国、政府、制度、事件、文化、道具、概念
- 例: `藤原氏`、`平氏`、`源氏`、`元`、`江戸幕府`、`明治政府`、`応仁の乱`

必須仕様:

- 説明文は50字以上150字以内を目安にする。
- 説明文は小学5年生までに理解しやすい言葉を中心にする。
- 難しい漢字・一般的ではない語にはルビを付ける。
- 「重要語です」「考える手がかりになります」だけの汎用説明にしない。
- その用語が、いつ、どこで、何を変えたのかが分かる説明にする。
- 1つの詳細説明の中で同じ用語が複数回出る場合、太字・クリック化するのは最初の1回だけにする。
- 人物名とアクションカード用語が重なる場合は、実在個人を人物カード、集団・制度・出来事をアクションカードとして明確に分ける。
- `元` は一文字で誤検出しやすいため、`元が大軍`、`元の襲来`、`元寇` など歴史用語として出る場合だけクリック化する。

表示仕様:

- 詳細本文中の重要語を太字かつクリック可能にする。
- クリックすると人物カードと同じモーダルで開く。
- アクションカードのモーダルでは、アイコンは原則 `💡` を使う。
- モーダルには、用語名、分類・要約、説明文、タグを表示する。
- 人物カードとアクションカードは同じモーダルUIを使うが、内容分類は別物として扱う。

追加・修正時の確認:

- その語が人物ではなく、出来事・制度・集団・文化・社会変化であるか確認する。
- 説明文が50字以上150字以内か確認する。
- いつ、どこで、何が変わったかが説明されているか確認する。
- 小学生向けに難しすぎる言い回しになっていないか確認する。
- 同じ詳細説明内で同じ語が複数クリック化されていないか確認する。
- 人物カードに入れるべき個人名を誤ってアクションカードに入れていないか確認する。

## 10. 詳細本文とカード連動の仕様

各時代の `くらし`、`できごと`、`大きな力` の詳細本文では、重要人物と重要語を学習導線として使う。

必須仕様:

- 重要人物は人物カードへつなげる。
- 重要語、出来事、制度、集団はアクションカードへつなげる。
- 王国・勢力・その他カードにある国名・王朝名・勢力名が説明本文や詳細本文に出る場合は、王国・勢力・その他カードへつなげる。
- 大カテゴリー、時代カード、子カテゴリーのサブタイトルや見出し補足文では、人物・アクション・王国カードへの本文リンクを付けない。リンク化は説明本文・詳細本文に限定する。
- 同じ詳細説明内で同じ語は最初の1回だけクリック可能にする。
- 太字にした語は、クリック時に必ず対応するカードが開くようにする。
- クリックできない太字を大量に増やさない。
- 文章は原則1000字以内に収める。
- 500字以内指定のUI部分では、その制限を守る。
- 詳細を開いた地点から縦に約500pxスクロールしたら自動で閉じる仕様を維持する。
- 詳細は、`くらし`、`できごと`、`大きな力` のカード内ではなく、カードの下に表示する。
- カードの位置は詳細開閉で不自然に動かさない。

確認項目:

- 詳細ボタンを押すとカード下に詳細が開く。
- 人物名クリックで人物モーダルが開く。
- 重要語クリックでアクションモーダルが開く。
- 同じ語が同じ説明内で二重リンク化されていない。
- ルビが必要な語に付いている。
- 小学生向けとして難しすぎる語は説明またはルビで補われている。
